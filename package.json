{
  "name": "zene-website-fallback",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist www www/.vitepress/dist && cp -r web/* dist/ && cp -r web/* www/ && cp -r web/* www/.vitepress/dist/ || true",
    "docs:build": "mkdir -p dist www www/.vitepress/dist && cp -r web/* dist/ && cp -r web/* www/ && cp -r web/* www/.vitepress/dist/ || true",
    "deploy": "echo 'No deploy needed'"
  }
}
