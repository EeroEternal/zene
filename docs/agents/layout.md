# Repository layout

Repo-level executable scripts go in [`scripts/`](../../scripts/) (for example `scripts/install.sh`, `scripts/install-release.sh`). Do not dump install or ops scripts at the repo root. Cloud subproject scripts stay in [`cloud/scripts/`](../../cloud/scripts/).
