{
  "name": "zene-www-legacy-fallback",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist .vitepress/dist && cp -r ../web/* dist/ && cp -r ../web/* .vitepress/dist/ || true",
    "docs:build": "mkdir -p dist .vitepress/dist && cp -r ../web/* dist/ && cp -r ../web/* .vitepress/dist/ || true"
  }
}
