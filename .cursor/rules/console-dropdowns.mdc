---
description: Console 下拉选择须用应用内弹出层，禁止原生 select
globs: cloud/apps/web/**/*.{tsx,ts}
alwaysApply: false
---

# 下拉选择（Picker）

禁止在 Console 使用原生 HTML 下拉控件（`<select>` / `<option>`）。浏览器 / OS 默认样式与 Console 设计系统不一致。

所有「从若干选项中选一项」的交互，必须用应用内弹出层实现：

- **触发器**：按钮或 chip，展示当前值 + chevron；样式与表单输入一致（`INPUT_CLASS` 或 `chipClass`）。
- **弹出层**：`rounded-md border border-line bg-canvas shadow-menu`；选项用 `picker-item` / `picker-item-active`；选中项配 `IconCheck`。
- **定位**：简单列表可锚定在触发器旁（`absolute`）；选项多或需搜索时用 `fixed` 面板（`useAnchoredMenu`）。
- **关闭**：点选项、Esc、或点 backdrop / 外部区域关闭。
- **可访问性**：触发器设 `aria-haspopup="menu"`、`aria-expanded`；面板设 `role="menu"`。

复用 `components/ui`（`SearchablePicker` / `FieldSelect` / `Menu`）和 `components/pickers`（`ProjectPicker` / `BranchPicker` / `ModelPicker`）。按能力 id 查 `cloud/apps/web/lib/capabilities.ts` 再 import，不要在页面里再手写一套 picker。

```tsx
// ❌ BAD — 原生 select，OS 样式
<select value={id} onChange={(e) => setId(e.target.value)}>
  {items.map((i) => <option key={i.id} value={i.id}>{i.label}</option>)}
</select>

// ✅ GOOD — 按钮触发 + 应用内 picker 面板
<button type="button" aria-haspopup="menu" aria-expanded={open} onClick={() => setOpen(!open)}>
  {label} <IconChevronDown />
</button>
{open && (
  <div role="menu" className="… shadow-menu">
    {items.map((i) => (
      <button key={i.id} type="button" className="picker-item" onClick={() => { setId(i.id); setOpen(false); }}>
        {i.label}
        {i.id === id && <IconCheck />}
      </button>
    ))}
  </div>
)}
```

新增选择交互时，优先用已有 `SearchablePicker` / `FieldSelect`，不要再引入 `<select>`。
