# zene-hooks

Lifecycle hook planning (`HookEngine`) and execution adapters (`HookExecutor`) for [Zene](https://github.com/ParaTensor/zene).

The engine builds run plans and interprets outcomes; subprocess IO lives in executor impls (default: `BashHookExecutor`).

## License

MIT
